# DocMind Lite - Environment Configuration
# Copy this file to .env and fill in your API keys

# =============================================================================
# REQUIRED: Aliyun DashScope API Key (Qwen-VL-Max)
# =============================================================================
# Get your key at: https://dashscope.console.aliyun.com/
# This is the primary API for PDF to Markdown conversion
DASHSCOPE_API_KEY=your-dashscope-api-key-here

# =============================================================================
# REQUIRED: OpenAI API Key (GPT-4o-mini)
# =============================================================================
# Get your key at: https://platform.openai.com/api-keys
# This is used to recover pages blocked by Qwen content filter
OPENAI_API_KEY=your-openai-api-key-here

# =============================================================================
# OPTIONAL: Concurrency Settings (optimized for 6GB RAM)
# =============================================================================
# Adjust these if you have more/less RAM available
#
# SEMAPHORE: Number of PDF chunks processed simultaneously
#   - Higher = faster but more memory
#   - Default: 4 (uses ~4.5GB RAM)
#   - For 8GB RAM: try 5-6
#   - For 4GB RAM: use 2
#
# LLM_CONCURRENT: LLM API calls per chunk
#   - Higher = faster but more memory
#   - Default: 6
#
# Memory estimation formula:
#   Total RAM = 1.5GB (base) + SEMAPHORE * 0.76GB
#
# Examples:
#   SEMAPHORE=2, LLM_CONCURRENT=4  -> ~3.0GB RAM (conservative)
#   SEMAPHORE=4, LLM_CONCURRENT=6  -> ~4.5GB RAM (default)
#   SEMAPHORE=6, LLM_CONCURRENT=8  -> ~6.0GB RAM (aggressive)

# SEMAPHORE=4
# LLM_CONCURRENT=6
